return {
  editor = {
    autoactivate = false,
    autoreload = true,
    autotabs = false,
    tabwidth = 2,
    usetabs = false,
    whitespace = false,
  },
  repl = {
    fontname = nil,
    fontsize = nil,
    nomousezoom = false,
  },
  activateoutput = true, -- activate output/console on Run/Debug/Compile
  stylesoutshell = nil,
  debugger = {
    maxdatalength = 256,
  }
}
